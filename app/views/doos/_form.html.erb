<%= form_with(model: doo) do |form| %>
  <% if doo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(doo.errors.count, "error") %> prohibited this doo from being saved:</h2>

      <ul>
        <% doo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.submit "投稿" %>
  </div>
<% end %>
