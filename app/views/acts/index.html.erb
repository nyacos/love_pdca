<p style="color: green"><%= notice %></p>

<h1>Acts</h1>

<div id="acts">
  <% @acts.each do |act| %>
    <%= render act %>
    <p>
      <%= link_to "Show this act", act %>
    </p>
  <% end %>
</div>

<%= link_to "New act", new_act_path %>
